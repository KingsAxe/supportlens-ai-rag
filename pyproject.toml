[project]
name = "supportlens-ai-rag"
version = "0.1.0"
description = "Phase 0 scaffold for a customer support intelligence RAG agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]